machine:
  services:
    - docker
test:
  pre:
    - docker pull elasticsearch:2.3
    - docker run -d -p 9200:9200 elasticsearch:2.3
  override:
    - wget --tries 10 --timeout=15 --read-timeout=20 --waitretry=10 --retry-connrefused 'localhost:9200'
