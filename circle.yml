test:
  pre:
    - docker pull levlaz/elasticsearch-docker-ci:5.1.1
    - docker run -d -p 9200:9200 levlaz/elasticsearch-docker-ci:5.1.1
  override:
    - wget --tries 180 --timeout=15 --read-timeout=20 --waitretry=1 --retry-connrefused 'localhost:9200'
